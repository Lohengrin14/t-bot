import React, { FC } from 'react';
import styles from './ReplyPage.module.css'

const ReplyPage: FC = () => {
    return (
        <h1 className={styles.body}>Reply page</h1>
    );
};

export default ReplyPage;