import React, { FC } from 'react';
import styles from './SamplePage.module.css'

const SamplePage: FC = () => {
    return (
        <h1 className={styles.body}>Sample page</h1>
    );
};

export default SamplePage;